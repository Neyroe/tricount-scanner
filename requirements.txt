fastapi
uvicorn[standard]
python-multipart
pdfplumber
python-dotenv
tricount-api

# Fenêtre native (app.py) — optionnel, le mode navigateur n'en a pas besoin.
pywebview
# Backend WebView2 de pywebview sous Windows.
pythonnet>=3.0 ; sys_platform == "win32"
